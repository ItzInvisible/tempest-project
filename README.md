# React + TypeScript + Vite + BaseUI

This is a template for a new Vite project with React, TypeScript, and BaseUI
(derived from shadcn/ui). The components have been converted to use BaseUI
primitives and patterns.
